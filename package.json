{
  "name": "telecast-tv",
  "version": "1.0.0",
  "private": true,
  "description": "Telecast live TV Android app",
  "scripts": {
    "build:android": "npx cap sync android && cd android && gradle assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3",
    "@capacitor/android": "^7.4.3",
    "@capacitor/cli": "^7.4.3"
  }
}
